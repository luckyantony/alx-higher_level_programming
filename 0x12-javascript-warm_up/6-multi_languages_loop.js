#!/usr/bin/node
// Print 3 lines using an array of string and a loop
const myVar = ['C is fun', 'Python is cool', 'JavaScript is amazing'];
for (let myIndex = 0; myIndex < 3; myIndex++) {
  console.log(myVar[myIndex]);
}